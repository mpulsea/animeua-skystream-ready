name: Build and Deploy Repository

on:
  push:
    branches: [ main ]
    paths-ignore:
      - 'dist/**'
      - 'repo.json'
      - 'repo-v2.json'
      - 'artwork/**'
      - 'animeua/poster-map.json'
  schedule:
    - cron: '17 */6 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: animeua-build-main
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'

      - name: Cache AnimeUA artwork
        run: node scripts/cache-animeua-artwork.mjs

      - name: Deploy SkyStream repository
        run: |
          npm install -g skystream-cli
          skystream deploy -u https://raw.githubusercontent.com/${{ github.repository }}/main
          cp dist/plugins.json dist/plugins-v2.json

      - name: Commit generated files and artwork
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore: refresh AnimeUA artwork and deploy [skip ci]" || echo "No changes to commit"
          git push
